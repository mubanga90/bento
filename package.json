{
  "name": "bento",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "watch": "nodemon -w src -e ts -x  'npm run test'",
    "build": "xo && tsc && vite build",
    "preview": "vite preview",
    "test": "xo",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.10",
    "nodemon": "^3.0.3",
    "typescript": "^5.2.2",
    "vite": "^5.0.8",
    "xo": "^0.57.0"
  }
}
